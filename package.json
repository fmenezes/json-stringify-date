{
  "name": "json-stringify-date",
  "version": "0.1.6",
  "description": "Like JSON.stringify, but preserve timezones in date objects and parse dates into Date object",
  "main": "index.js",
  "dependencies": {
    "json-stringify-safe": "~5.0.0",
    "moment": "^2.9.0"
  },
  "devDependencies": {
    "jslint": "^0.12.0",
    "mocha": "^2.2.1",
    "nyc": "^13.1.0",
    "should": "^5.2.0"
  },
  "scripts": {
    "version": "sed -i '' -e \"s/## Next Release/## $(echo $npm_package_version), $(date +%Y-%m-%d)/\" CHANGELOG.md && git add CHANGELOG.md",
    "postversion": "git push && git push --tag",
    "test:coverage": "nyc npm test",
    "test": "mocha",
    "lint": "jslint *.js **/*.js"
  },
  "repository": "git://github.com/fmenezes/json-stringify-date",
  "keywords": [
    "json",
    "stringify",
    "date",
    "parse"
  ],
  "author": "Filipe Constantinov Menezes <fcmenezes87@gmail.com>",
  "license": "MIT",
  "nyc": {
    "check-coverage": true,
    "lines": 95
  }
}
